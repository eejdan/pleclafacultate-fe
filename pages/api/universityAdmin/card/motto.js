
import connectMongo from "../../../utils/connectMongo";

export default function handler(req, res) { 
    function handlePUT() {

    }
    function handleGET() {
        let body = req.body;
        
    }
    switch(req.method) {
        case 'PUT':
            break;
        case 'GET':
            break;
        default:
            break;
    }
}